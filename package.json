{
  "name": "resume",
  "author": "Amanpreet Singh",
  "version": "0.0.2",
  "scripts": {
    "build": "esbuild --bundle src/GameManager.js --outfile=www/main.js --minify",
    "dev": "esbuild --bundle src/GameManager.js --outfile=www/main.js --servedir=www"
  },
  "dependencies": {
    "kaboom": "^3000.1.17"
  },
  "devDependencies": {
    "esbuild": "^0.20.1"
  }
}
