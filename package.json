{
  "name": "resume",
  "scripts": {
    "build": "esbuild --bundle src/main.js --outfile=www/main.js --minify",
    "dev": "esbuild --bundle src/main.js --outfile=www/main.js --servedir=www",
    "bundle": "npm run build && mkdir dist && 7z a dist/game.zip www"
  },
  "dependencies": {
    "kaboom": "^3000.1.17"
  },
  "devDependencies": {
    "esbuild": "^0.20.1"
  }
}
